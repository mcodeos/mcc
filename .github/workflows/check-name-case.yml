name: check-name-case

on:
  push:
  pull_request:

jobs:
  scan:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Scan src for unregistered case-insensitive name comparisons
        run: python3 scripts/check-name-case.py
