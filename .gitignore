.DS_Store
.claude/settings.json
debug
target
Cargo.lock
# The batch counter is shared and lives in the ledger repo: mcd/BUILDNr
**/*.rs.bk
*.pdb
**/mutants.out*/
baseline
projects
mcode

# Local workspace artifacts / directories — never upload
.trae/
plan/
test/
unitest/
circuit.html
*.html
mcc-cli
mcs

# Local authored content — keep out of the upstream repo
mcd/
mclibs/
t/
MCODE-AI-RULES.md
mcext-main